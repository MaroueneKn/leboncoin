//
//  leboncoinTests.swift
//  leboncoinTests
//
//  Created by Kandara Marouene on 06/11/2022.
//

import XCTest
@testable import leboncoin

final class leboncoinTests: XCTestCase {

    override func setUpWithError() throws {
        // Put setup code here. This method is called before the invocation of each test method in the class.
    }

    override func tearDownWithError() throws {
        // Put teardown code here. This method is called after the invocation of each test method in the class.
    }

    
}



/* let viewModel = ListItemsViewModel()
 let items = viewModel.allItems
 if let title = items.first?.title {
     let firstElement = NSPredicate(format: "label contains '\(title)'")
     app.tables.buttons.element(matching: firstElement).tap()
     XCTAssert(app.staticTexts[title].exists)
     app.navigationBars.buttons.element(boundBy: 0).tap()
 }
 if let title = items[1].title {
     let secondElement = NSPredicate(format: "label contains '\(title)'")
     app.tables.buttons.element(matching: secondElement).tap()
     XCTAssert(app.staticTexts[title].exists)
     app.navigationBars.buttons.element(boundBy: 0).tap()
 }
*/
